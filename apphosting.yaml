# Firebase App Hosting configuration
# https://firebase.google.com/docs/app-hosting/configure

backend:
  runtime: nodejs

runConfig:
  concurrency: 80
  cpu: 1
  memoryMiB: 512
  minInstances: 0
  maxInstances: 2

env:
  - variable: NODE_ENV
    value: production
    availability:
      - RUNTIME
